# Production — Instance-1 (OVH Public Cloud)
# Worker runs separately on Instance-2. No MinIO (uses real OVH Object Storage).
# Copy .env.example to .env.prod, fill in all values, then:
#   docker compose -f docker-compose.prod.yml up -d

services:
  db:
    image: postgres:15-alpine
    restart: always
    env_file: .env.prod
    environment:
      POSTGRES_DB: sitephoto
      POSTGRES_USER: sitephoto
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-db.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sitephoto"]
      interval: 5s
      timeout: 5s
      retries: 10

  app:
    build: .
    restart: always
    env_file: .env.prod
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  redis:
    image: redis:7-alpine
    restart: always
    # Bound to loopback + vRack private IP — never exposed on the public interface.
    # Set REDIS_BIND_IP to the vRack IP of Instance-1 (e.g. 10.0.0.1).
    command: >
      redis-server
      --requirepass ${REDIS_PASSWORD}
      --bind 127.0.0.1 ${REDIS_BIND_IP}
    ports:
      - "${REDIS_BIND_IP}:6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  caddy:
    image: caddy:2-alpine
    restart: always
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    env_file: .env.prod
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - app

volumes:
  postgres_data:
  redis_data:
  caddy_data:
  caddy_config:
